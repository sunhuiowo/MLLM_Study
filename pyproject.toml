[project]
name = "yolo-to-vlm"
version = "0.1.0"
description = "YOLO to ms-SWIFT grounding format converter"
requires-python = ">=3.10"
dependencies = [
    "pillow>=12.1.1",
    "pyyaml>=6.0.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.0",
]

[tool.uv]
dev-dependencies = []
